import React from 'react';
import { withTranslation } from 'react-i18next';

/*
  TODO: implementation of this module
  FolderStats: module that renders stats of the selected campaign. Useful as incentive and feedback for users.
 */
class FolderStats extends React.Component {
  render() {
    return <div className="folder-edit-container">
      UNDER DEVELOPMENT
    </div>;
  }
}

export default withTranslation()(FolderStats);